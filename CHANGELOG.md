# Changelog

## 2.0.0

- Configure supported clients with direct HTTP OAuth or `SPEKO_API_KEY`.
- Remove the `bridge` command, `mcp-remote`, and legacy stdio configuration;
  OAuth-capable clients now use native HTTP discovery.
- Add direct config builders for modern MCP clients.
